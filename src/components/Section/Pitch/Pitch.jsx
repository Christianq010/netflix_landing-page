import React from 'react';

import './styles.module.scss';

const pitch = () => {
    return (
      <section>
        <h1 className="pitchTitle">See what's next.</h1>
        <p className="pitchSubtitle">WATCH ANYWHERE. CANCEL ANYTIME.</p>
        <button className="joinButton">JOIN FREE FOR A MONTH</button>
      </section>
    )
}

export default pitch;